export const editList = ({ editList: { list } }) => list;
export const editListPagesCount = ({ editList: { pagesCount } }) => pagesCount;
export const editAllPages = ({ editList: { allPages } }) => allPages;
export const editType = ({ editList: { type } }) => type;
export const editSelectList = ({ editList: { selectList } }) => selectList;
